/**
 * Eidos Language OS - Player Header Bar (Alias for PlayerStudioHeader)
 */
export { PlayerStudioHeader as PlayerHeaderBar } from './PlayerStudioHeader';
export { PlayerStudioHeader } from './PlayerStudioHeader';
